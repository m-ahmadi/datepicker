{
  "name": "MyApp",
  "version": "0.0.1",
  "scripts": {
    "build": "node build.js",
    "toggleLive": "node build.js toggleLive",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "chokidar": "latest",
    "livereload": "latest",
    "htmlbilder": "latest",
    "sass": "latest",
    "rtlcss": "latest"
  }
}
